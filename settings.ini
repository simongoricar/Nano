[Credentials]
token = 

[Settings]
ownerid = 

[Dev]
server = 

[Storage]
# legacy = old way of writing to servers.yml (highly recommended if you host the bot yourself!)
# redis = uses a Redis server (must already be running)
type = redis

[Redis]
# openshift = setup based on openshift's redis cardtidge (does not need ip, port or password)
# manual = uses values below or defaults if empty
setup = manual
ip = localhost
port = 6379
password = 